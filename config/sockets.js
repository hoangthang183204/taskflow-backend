// config/sockets.js
module.exports.sockets = {
  // ============================================================
  // ✅ CORS
  // ============================================================
  onlyAllowOrigins: [
    "http://localhost:3000",
    "http://localhost:3001",
    "http://127.0.0.1:3000",
    process.env.FRONTEND_URL,
  ].filter(Boolean),

  // ============================================================
  // ✅ TRANSPORTS
  // ============================================================
  transports: ["polling", "websocket"],

  // ============================================================
  // ✅ TIMEOUT
  // ============================================================
  pingTimeout: 60000,
  pingInterval: 25000,

  // ============================================================
  // ❌ KHÔNG có beforeConnect — để bootstrap xử lý
  // ============================================================
  // beforeConnect: undefined,
};